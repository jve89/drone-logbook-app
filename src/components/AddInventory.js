import React from 'react';

function AddInventory() {
  return (
    <div className="w-full max-w-2xl rounded bg-white p-6 shadow">
      <h2 className="mb-4 text-2xl font-bold">Add Inventory</h2>
      <p>
        Placeholder page for adding new inventory items (drones, batteries,
        controllers, etc.).
      </p>
    </div>
  );
}

export default AddInventory;
